This project is a simple Python command line scorebook application that allows users to manage student grades. The program lets you add students, list all records, compute class statistics and save/load data from a CSV file. To run the application, open a terminal in the project folder and run python scorebook.py. Use the menu displayed in the terminal to interact with the program. The grades are stored in grades.csv, which is automatically created when you choose the save option.